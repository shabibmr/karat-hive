# Guest-first launch — task register

| | |
|---|---|
| **Source** | [`guest-launch-plan.md`](guest-launch-plan.md) · frozen consensus in [`app-launch-flow-review.md`](app-launch-flow-review.md) |
| **Does not override** | SRS v1.3 · `adr/0010` · Architecture-Frontend · domain invariants |
| **App** | `apps/kh_mobile/karat_hive` only |
| **Prefix** | `GL-*` — Guest Launch. Stable, never reused. Ids `GL-00`…`GL-79` only. |
| **Date** | 11 September 2026 |

This file is the work list: IDs, order, files, acceptance. Design lives in the plan. Product rules stay frozen in the consensus review.

**Legend:** `[ ]` open · `[-]` in progress · `[x]` done · `[~]` blocked.

**Atomic rule:** one action, one done-when. If a row needs two behaviours, it is two rows.

**Register hygiene.** Only the **orchestrator** ticks Status. Implementers do not edit this file. Tick after verifying the tree (on disk or HEAD), not from a worker’s claim.

**Current tree (11 Sep 2026, orchestrator pass):** Waves 0–6 landed. `GL-00`–`GL-76` **done** (`flutter analyze` clean on touched files; Wave 6 suites green; GL-76 checklist signed off in plan). `GL-77`–`GL-79` **blocked** on unauth lookup probes.

---

## Wave 0 — Documents

| Task | Files | Action | Deps | Done when |
|------|--------|--------|------|-----------|
| `[x]` **GL-00** write plan | `[NEW]` `apps/kh_mobile/karat_hive/docs/guest-launch-plan.md` | Copy this design into the repo. | — | File exists; frozen rules + key design + file list present. |
| `[x]` **GL-01** write register | `[NEW]` `apps/kh_mobile/karat_hive/docs/guest-launch-tasks.md` | Copy this `GL-*` table. | GL-00 | File exists; ids GL-00…GL-79 match this plan. |
| `[x]` **GL-02** pointer | `docs/app-launch-flow-review.md` | Add “Implementation: guest-launch-plan.md / guest-launch-tasks.md” at top. Do not rewrite consensus. | GL-00, GL-01 | Pointer present. |

## Wave 1 — Routing & session (placeholder Guest OK)

| Task | Files | Action | Deps | Done when |
|------|--------|--------|------|-----------|
| `[x]` **GL-03** constant | `lib/app/guards.dart` | Add `guestLanding = '/guest'`. | GL-01 | Constant exists. |
| `[x]` **GL-04** unauth set | `guards.dart` | Add `guestLanding` to `unauthRoutes`. | GL-03 | Set membership only. |
| `[x]` **GL-05** SignedOut default | `guards.dart` `redirect` | `SignedOut` + not an allowed unauth/create path → `guestLanding` (not `customerOnboarding`). | GL-04 | Unit: unknown path → `/guest`. |
| `[x]` **GL-06** placeholder screen | `[NEW]` `guest_landing_screen.dart` | Empty `KhScaffold` + `Key('guest-landing')`. No cards yet. | GL-03 | Widget builds. |
| `[x]` **GL-07** mount placeholder | `features/auth/routes.dart` | `GoRoute` for `guestLanding` → placeholder. | GL-06 | Route registered. |
| `[x]` **GL-08** create-path helper | `guards.dart` | `isGuestCreateLocation(location)` for the seven create paths (type, ornament, sell-gold, coins, bullion, images, review). | GL-03 | Helper unit-tested with true/false examples. |
| `[x]` **GL-09** SignedOut may create | `guards.dart` | `SignedOut` + create path → `null` (stay). | GL-05, GL-08 | Unit: `/customer/requests/create/ornament` allowed. |
| `[x]` **GL-10** SignedOut private tabs | `guards.dart` | `SignedOut` + home / requests list / connections / alerts / profile → `guestLanding`. | GL-05 | Unit: each private path redirects to `/guest`. |
| `[x]` **GL-11** mount create for guest | `router.dart` | Create routes reachable while `SignedOut` (lift out of `CustomerShell`; do not duplicate paths — see plan). Customer shell tabs still `SignedIn` only. | GL-09 | Guest can `go` a create path without hitting Customer shell auth. |
| `[x]` **GL-12** intent holder | `[NEW]` small provider (e.g. `pending_publish_intent.dart`) | Bool/flag: pending publish yes/no. Default false. | GL-01 | Provider readable from guards. |
| `[x]` **GL-13** intent set/clear | same | `setPending()`, `clearPending()`. No persistence. | GL-12 | Unit: set then clear. |
| `[x]` **GL-14** Customer + intent stays on create | `guards.dart` | `SignedIn` Customer + pending + create path → `null`. Do **not** send to `customerHome`. | GL-09, GL-12 | Unit test. |
| `[x]` **GL-15** Customer + intent off create | `guards.dart` | `SignedIn` Customer + pending + `splash` / `customerOnboarding` / `customerRegister` (any unauth Login/signup door — **not** `AppGuards.login`) → create review path (or current create path), not Dashboard. | GL-14 | Unit: pending Customer on each of those three locations stays on create. |
| `[x]` **GL-16** Vendor + intent | `guards.dart` | `SignedIn` Vendor + pending → `clearPending`; then normal vendor redirect (home or awaiting). | GL-12, GL-13 | Unit: intent false after; location is vendor, not create. |
| `[x]` **GL-17** UnboundGoogle landing | `guards.dart` | Unbound → `customerRegister` (or existing completion route), **not** `authComplete`. | GL-05 | Unit: unbound + `/` → signup route. |
| `[x]` **GL-18** completer not default | `guards.dart` | `/auth/complete` may remain registered; it is not the SignedOut/Unbound default. | GL-17 | Unit: unbound default ≠ `authComplete`. |
| `[x]` **GL-19** tests splash / blocked | `test/app/guards_test.dart` | Keep SessionLoading → splash; AuthBlocked → blocked. | GL-05 | Those tests green. |
| `[x]` **GL-20** tests SignedOut default | `guards_test.dart` | Replace onboarding-as-home assertions with guest landing. | GL-05, GL-07 | Green. |
| `[x]` **GL-21** tests create vs private | `guards_test.dart` | SignedOut create allowed; private tabs → guest. | GL-09, GL-10 | Green. |
| `[x]` **GL-22** tests pending publish | `guards_test.dart` | Customer keeps create; Vendor clears + vendor dest. | GL-14, GL-16 | Green. |
| `[x]` **GL-23** tests vendor restore | `guards_test.dart` | Active → vendor home; pending/rejected/verified → awaiting/KYC unchanged. | GL-05 | Green. |
| `[x]` **GL-24** tests Customer restore | `guards_test.dart` | SignedIn Customer no intent → Dashboard; not Guest. | GL-05 | Green. |

## Wave 2 — Guest Landing UI

| Task | Files | Action | Deps | Done when |
|------|--------|--------|------|-----------|
| `[x]` **GL-25** ARB keys | `packages/kh_l10n` | Add guest strings: 4 titles, 4 how-it-works, Log in, jeweller footer. All locales the package already has. | GL-00 | Keys exist; gen-l10n succeeds. |
| `[x]` **GL-26** HowThisWorks widget | `[NEW]` small widget | Shared steps: post → offers → accept → WhatsApp. Takes extra type-specific lines. | GL-25 | Widget test with one extra line. |
| `[x]` **GL-27** Landing chrome | `guest_landing_screen.dart` | Real scaffold, **no** bottom nav, title from l10n. | GL-06, GL-25 | Key `guest-landing` still present. |
| `[x]` **GL-28** four cards | same | Four tappable cards + keys: `guest-type-ornament`, `sell-gold`, `coins`, `bullion`. | GL-27 | Widget test finds 4 keys. |
| `[x]` **GL-29** how-it-works on cards | same | Each card uses `HowThisWorks` with type extras (bullion minimum). | GL-26, GL-28 | Widget test: how-it-works visible or expandable per card. |
| `[x]` **GL-30** Log in action | same | Top-right text button `Key('guest-login')`. Navigation stub OK (`/customer/onboarding` as Login). | GL-27 | Widget test: button exists. |
| `[x]` **GL-31** jeweller footer | same | Footer text link `Key('guest-jeweller')`. Navigation stub to register. | GL-27 | Widget test: link exists; not a filled primary button beside the 4 cards. |
| `[x]` **GL-32** Landing widget test file | `[NEW]` `test/features/guest_landing_screen_test.dart` | Covers GL-28–GL-31. | GL-28–GL-31 | `flutter test` that file green. |
| `[x]` **GL-33** tap sets type | Landing + `RequestCreateController` | Tap card writes that `RequestType` into the keep-alive create controller. | GL-28, GL-11 | Controller unit: type matches tap. |
| `[x]` **GL-34** tap opens compose | Landing | Tap card `go`s the matching create compose path. | GL-11, GL-33 | Widget/nav test: ornament card → ornament path. |

## Wave 3 — Login doors

| Task | Files | Action | Deps | Done when |
|------|--------|--------|------|-----------|
| `[x]` **GL-35** extract Google panel | `[NEW]` shared widget from onboarding | Continue-with-Google + busy + cancel/idle. No biometric required. | GL-30 | Onboarding or Login can host it. |
| `[x]` **GL-36** Login route body | login or onboarding screen | Guest corner uses this as **Login**, not as home. Reuse `/customer/onboarding`. | GL-35, GL-07 | Opening Login does not show 4 service cards. |
| `[x]` **GL-37** wire corner Log in | Guest Landing | `guest-login` → Login route. | GL-30, GL-36 | Nav test. |
| `[x]` **GL-38** Google cancel | onboarding/login controller | Cancel/dismiss → idle; **no** error banner. | GL-35 | Unit: state idle after cancel. |
| `[x]` **GL-39** Google network fail | same | Fail → inline error; stay on Login. | GL-35 | Unit: failure state. |
| `[x]` **GL-40** Customer corner success | guards + session | No pending intent → Customer Dashboard. | GL-24, GL-36 | Test or guard: no pending + `customerOnboarding` → Dashboard. `AppGuards.login` stays Vendor OTP only. |
| `[x]` **GL-41** Vendor corner success | guards | No pending → vendor home or awaiting/KYC. | GL-23, GL-36 | Same as today’s vendor restore. |
| `[x]` **GL-42** choose one signup | comment + one controller | Keep **CustomerCompletionController** as the one signup; register screen becomes a wrapper that `begin()`s with the Firebase ID token. Do not rely on `_CompletionView` still mounted. Token must survive UnboundGoogle redirect (session field, onboarding controller, or small holder) — completion is AutoDispose, create is keep-alive. | GL-17 | Only one form is reachable from UnboundGoogle; wrapper `begin()` has a token. |
| `[x]` **GL-43** unbound → that signup | guards + routes | UnboundGoogle location is `customerRegister`. Keep the Firebase ID token available at that route (do not drop it when leaving onboarding). | GL-17, GL-42 | Unit: unbound + `/` → signup; token present for `begin()`. |
| `[x]` **GL-44** signup success, no intent | completion controller | After register bundle → Dashboard. | GL-42, GL-40 | Controller sets session; guard sends home. |
| `[x]` **GL-45** completer unused on this path | `auth_completer_screen.dart` | Do not link from Guest/Login/Publish. Leave file; no delete in this wave. | GL-18 | Grep: Guest/Login do not `go(authComplete)`. |
| `[x]` **GL-46** Guest Publish sets intent | create controller | If `SignedOut` and user taps Publish: `setPending()`, do **not** `saveDraft`. | GL-13 | Unit: intent true; no repo createDraft. |
| `[x]` **GL-47** show Login overlay | review/publish screen | Guest Publish opens Login as **overlay/dialog/push that does not reset** `RequestCreateController`. | GL-36, GL-46 | Overlay shown. |
| `[x]` **GL-48** dismiss Login overlay | same | Back/cancel closes overlay; form fields unchanged; stay guest. | GL-47, GL-38 | Widget test: entered field still there. |
| `[x]` **GL-49** overlay test | `[NEW]` or create tests | Covers GL-47–GL-48. | GL-48 | Test green. |

## Wave 4 — Guest compose + auto-publish

| Task | Files | Action | Deps | Done when |
|------|--------|--------|------|-----------|
| `[x]` **GL-50** skip `me()` when guest | `request_create_controller.dart` | `loadLookups`: if not `SignedIn`, do not call `me()`. | GL-11 | Unit with mock: `me()` not invoked. |
| `[x]` **GL-51** lookups ready without me | same | Config/categories/regions success → `lookupsReady` even if rates missing (today’s rates rule). | GL-50 | Unit. |
| `[x]` **GL-52** no cap-block from missing me | same | Guest `capBlocked` stays false. | GL-50 | Unit. |
| `[x]` **GL-53** guest add image local | create controller | Pick/capture stores local `File`; no uploader call. | GL-50 | Unit: media repo not called. |
| `[x]` **GL-54** guest remove image local | same | Remove by index; no `deleteMedia` API. | GL-53 | Unit. |
| `[x]` **GL-55** after-auth upload pending | same | Once `SignedIn`, upload local files before draft. | GL-53 | Unit: upload then draft order. |
| `[x]` **GL-56** saveDraft only signed-in | same | Guest `saveDraft`/`publish` API not called. | GL-46 | Unit. |
| `[x]` **GL-57** auto-publish trigger | create controller or binder | `SignedIn` Customer + pending → start pipeline **once**. | GL-14, GL-46 | Unit: one shot (no loop). |
| `[x]` **GL-58** pipeline steps | same | Upload pending → `saveDraft` → `publish` (existing idempotency key). | GL-55, GL-57 | Unit happy path. |
| `[x]` **GL-59** pipeline API fail | same | Stay `SignedIn`; show form error; user can tap Publish again (already authed). | GL-58 | Unit: session not cleared. |
| `[x]` **GL-60** pipeline success | same | `clearPending`; success step as today. | GL-58, GL-13 | Unit: intent false. |
| `[x]` **GL-61** Vendor drops draft | create controller | On Vendor session + pending: reset create state; `clearPending`; **no** publish/draft call. | GL-16 | Unit. |
| `[x]` **GL-62** Vendor test no API | create tests | Mock repo: publish/createDraft count 0 on vendor gate. | GL-61 | Green. |
| `[x]` **GL-63** signup then pipeline | completion + GL-57 | New Google completes signup with pending still true → pipeline. | GL-44, GL-57 | Unit or widget: intent survives signup. |
| `[x]` **GL-64** create-guest test file | `[NEW]` `test/features/request_create/guest_publish_test.dart` | Covers GL-50–GL-63 as needed. | GL-50–GL-63 | File green. |

## Wave 5 — Jeweller, blocked, retire old home

| Task | Files | Action | Deps | Done when |
|------|--------|--------|------|-----------|
| `[x]` **GL-65** jeweller navigates register | Guest Landing | Footer → `AppGuards.register` (Vendor signup, not login). | GL-31 | Nav test. |
| `[x]` **GL-66** jeweller resets create | Landing or a small callback | Before/on navigate: reset `RequestCreateController`. | GL-33, GL-65 | Unit: type/fields empty after. |
| `[x]` **GL-67** jeweller drop test | guest landing or create test | Mid-create then jeweller → draft gone. | GL-66 | Green. |
| `[x]` **GL-68** lockout → AuthBlocked | session / onboarding controller | Suspended/deactivated still `AuthBlocked`, not Guest. | GL-39 | Unit. |
| `[x]` **GL-69** blocked ≠ guest | `guards_test.dart` | AuthBlocked never redirects to `/guest`. | GL-19, GL-68 | Green. |
| `[x]` **GL-70** CUS-S01 not default | guards | No remaining SignedOut → `customerOnboarding` default. Onboarding may still be Login body. | GL-05, GL-36 | Grep `redirect` + test. |

## Wave 6 — Verify

| Task | Files | Action | Deps | Done when |
|------|--------|--------|------|-----------|
| `[x]` **GL-71** analyze | mobile package | `flutter analyze` on touched files. | Wave 1–5 | No new errors in those files. |
| `[x]` **GL-72** test guards | `test/app/guards_test.dart` | Full file. | GL-19–GL-24, GL-69 | Green. |
| `[x]` **GL-73** test landing | `guest_landing_screen_test.dart` | Full file. | GL-32 | Green. |
| `[x]` **GL-74** test guest publish | `guest_publish_test.dart` | Full file. | GL-64 | Green. |
| `[x]` **GL-75** test auth signup/login | existing auth tests + updates | Onboarding/login/completion tests updated for new defaults. | GL-38–GL-45 | Green. |
| `[x]` **GL-76** manual checklist | `guest-launch-plan.md` | List: cold Guest; Customer token; Vendor active; Vendor awaiting; compose; login-at-publish; cancel login; jeweller drop; blocked. | GL-71 | Checklist cases executed and signed off in Wave 6 (section already present). |
| `[~]` **GL-77** probe config unauth | backend or local API | Call platform config without token. | GL-51 | Result recorded (pass or gap id). |
| `[~]` **GL-78** probe taxonomy unauth | same | Categories + regions without token. | GL-51 | Recorded. |
| `[~]` **GL-79** probe gold-rates unauth | same | Rates without token. | GL-51 | Recorded. If any of 77–79 fail, link a backend follow-up; do not fake client success. |
