# kh_mobile

Dual-mode Flutter app for **Customer** and **Vendor** (iOS + Android). Mode is the account's `user_type`, not an in-session switch.

Prescribed by `docs/Architecture-Frontend.md` §4–§5 (`AD-FE-02`, `C-08`, `C-10`). Do not put Admin screens here.
