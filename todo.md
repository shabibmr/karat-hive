## Login (ADM-S01)

- [ ] Google Sign-In spinner stops when Firebase `authenticate()` returns; KH backend exchange (`POST /v1/auth/google/session` in `SessionController._syncFirebaseUser`) still runs after. Login screen does not wait for KH tokens — user can tap again or see a form flash before redirect. Wait for `SessionState.authenticated` or error after Google; keep spinner until then.
- [ ] Google web OAuth client id is hardcoded in `apps/kh_admin/lib/core/firebase/firebase_auth_service.dart` (`132845397292-t8q9pjhr4jdrei8ha44b0lipjd1c5h5n.apps.googleusercontent.com`). Move to `--dart-define` (same pattern as `KH_API_BASE`).
- [ ] 2FA / TOTP is a commented TODO on the login form (`login_screen.dart`). Not built. Leave until backend requires it.
- [ ] Product confirmed: ADM-S01 is Google Sign-In only. Hide/remove email/password fields, Authenticate button, and OR divider from `apps/kh_admin/lib/features/auth/presentation/login_screen.dart`. Keep `KH_DEV_AUTOLOGIN` as a compile-time shortcut only (may still call `POST /v1/auth/login/password` in SessionController; that path must not appear on the login screen). Update `apps/kh_admin/test/features/auth/login_screen_test.dart` (current tests assume email/password).

## Dashboard (ADM-S02)

- [ ] Dashboard is still a thin snapshot only: 6 count tiles from `GET /v1/admin/dashboard` plus 1–2 rows each from verification / abuse / pending-review. Spec `FR-ADM-003`–`FR-ADM-009` also needs date-range selector (today / 7d / 30d / 90d / custom), period stats, trends/charts, vendor-state breakdown, request/offer/connection rates, and indicative gold-weight stats (labelled indicative). Build in `apps/kh_admin/lib/features/dashboard/` — not done yet.
- [ ] Metric taps open the list with no filter (`FR-ADM-003` AC4). Customers → `/customers` (unfiltered). Vendors → `/vendors` (not pending KYC). KYC → `/verification`. Queue action buttons also go to the list, not the specific row (verification could use `?selectedId=`). See `apps/kh_admin/lib/features/dashboard/model/dashboard_queue_item.dart`, `apps/kh_admin/lib/features/dashboard/presentation/dashboard_screen.dart`.
- [ ] “SYSTEM OPERATIONAL” chip on `apps/kh_admin/lib/features/dashboard/presentation/dashboard_screen.dart` is always green. It does not call a health endpoint.
- [ ] Verification snapshot in `apps/kh_admin/lib/features/dashboard/repository/dashboard_repository.dart` (`fetchVerificationSnapshot`) GETs the full `/v1/admin/verification-queue` then `.take(2)`. Abuse and reviews already pass `limit=2`. Align verification with a limit (or shared list helper).
- [ ] Queue source failures are swallowed (`_omitQueueSource` returns `[]`). Stats errors show a retry banner; a dead verification/abuse/review feed looks like an empty queue. Decide whether to show a small per-source warning.

## Customers (ADM-S03 / ADM-S04)

- [ ] List columns vs `FR-ADM-010`: table has name, email/phone, state, request count, joined date, View. Missing Region, Connection count, aggregate rating. Files: `apps/kh_admin/lib/features/customers/presentation/customer_list_screen.dart`, `customer_list_item.dart`.
- [ ] List filters vs spec: only search + account state. Missing Region, registration date range, activity level. Search already matches name/email/mobile via `q`. No URL query for filters (`ADM-SMP-63`). File: `customer_list_filters.dart`.
- [ ] Dual pagination on the list: both “Load More” (append) and prev/next page (replace). Confusing — pick one. Files: `customer_list_controller.dart`, `_CustomerPaginationControls`.
- [ ] Detail vs `FR-ADM-011`: has profile, request list, admin notes, suspend / reactivate / erasure. Missing Offers received, Connections, reviews written/received, abuse reports. Request rows do not navigate to `/requests/:id`. Dates are `YYYY-MM-DD` local/UTC, not GST (`kh_formats.dart` / BR-021). Files: `customer_detail.dart`, `customer_detail_screen.dart`.
- [ ] List PII banner is UI-only. Spec `FR-ADM-010` AC5 / `FR-SYS-011`: opening the list/record must be audit-logged (backend write). Confirm it happens on `GET /v1/admin/customers` and `GET /v1/admin/customers/:id`. Do not fake it in the Flutter banner.

## Vendors (ADM-S05 / ADM-S06)

- [ ] List vs `FR-ADM-013`: columns are business name, trade licence, Region, verification, account, offer count, acceptance rate, rating, action. Missing Categories and registration date. Filters are verification, account, search (`q`) only — missing Region, Category, date range. Pending rows show waiting hours if the API sends them; repo comment says list route does **not** send `region` or `oldestWaitingHours`, so those cells may stay `—`. Files: `apps/kh_admin/lib/features/vendors/presentation/vendor_list_screen.dart`, `vendor_list_item.dart`, `vendor_repository.dart` `normalizeListItem`.
- [ ] Dual pagination on the list: both “Load More” (append) and Previous/Next (replace), same as customers. Pick one. File: `vendor_list_controller.dart`.
- [ ] Detail vs `FR-ADM-014`: shows profile, categories/regions chips, KYC document **list** (type, filename, uploaded/expiry, verified chip) — no document viewer, no signed URL open. No verification history, offer history, connections, reviews, or performance metrics (acceptance rate, response time, expiry rate, rating trend). No verify / reject / request-more-docs here (those live on Verification Queue). Dates `YYYY-MM-DD`, not GST. Files: `vendor_detail_screen.dart`, `vendor_detail.dart`.
- [ ] Reactivate uses hardcoded reason `ADMIN_REACTIVATED` / `KYC verified` (`vendor_detail_controller.dart`) after a confirm dialog — `FR-ADM-016` AC5 wants a reason on every transition. Suspend and deactivate do collect reason code + text.
- [ ] KYC document view is not logged in the client (`NFR-015`). Confirm backend logs GET of document URLs; Flutter currently cannot open the file.

## Firebase Messaging (closed-app push)

- [ ] Implement Firebase Cloud Messaging end-to-end so notifications reach Customer/Vendor users when the app is closed or backgrounded. Today: `firebase_messaging` is a dependency and `firebaseMessagingBackgroundHandler` is registered in `apps/kh_mobile/karat_hive/lib/bootstrap.dart`, but it only `debugPrint`s. `FirebaseNotificationService` (`apps/kh_mobile/karat_hive/lib/core/firebase/firebase_notification_service.dart`) never has `requestPermission`, `getToken`, or `initializeForegroundHandler` called. Flutter never `POST /v1/devices`, so the backend has no token to target. In-app centres are stubs (`CUS-S19` placeholder in `lib/features/notifications/presentation/stubs.dart`; `VEN-S17` is CP-5 / `CP5-B01`). Backend FCM adapter stubs `SENT` if `FCM_SERVER_KEY` is absent (`backend/src/platform/adapters/fcm/fcm-push.adapter.ts`). Scope: permission + token register/unregister on sign-in/out (`CP5-A08`), wire FCM handlers + deep links (`CP5-B02`), notification centre lists (`CP5-B01`), confirm `FCM_SERVER_KEY` in env. Spec: `FR-CUS-032`, `FR-VEN-026`, `FR-SYS-008`, `FR-ADM-029`.
