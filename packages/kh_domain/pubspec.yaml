name: kh_domain
description: Karat Hive domain model — pure Dart entities, value objects, enums.
publish_to: none
resolution: workspace
version: 0.1.0

environment:
  sdk: ">=3.9.0 <4.0.0"

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
