name: kh_domain
description: Karat Hive domain model — pure Dart entities, value objects, enums.
publish_to: none
resolution: workspace
version: 0.1.0

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0

dev_dependencies:
  build_runner: ^2.5.4
  freezed: 3.2.5
  json_serializable: ^6.11.1
  lints: ^5.0.0
  test: ^1.25.8
