name: kh_design_system
description: Karat Hive design system — tokens, theme, SH-FND-* / SH-AUTH-* / SH-MED-* primitives.
publish_to: none
resolution: workspace
version: 0.1.0

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
